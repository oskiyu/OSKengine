#pragma once

#include <vector>

namespace OSK::Collections {

	/*template <typename T> struct _VectorElementPtr {
		
		size_t elementIndex;

	};

	template <typename T> inline T GetElementFromPointer(const std::vector<T>& vec, const _VectorElementPtr<T>& ptr) {
		return vec[ptr.elementIndex];
	}*/

}
