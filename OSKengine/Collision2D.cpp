/*#include "Collision2D.h"

namespace OSK {

/*	bool CheckRectangleIntersection(const OSK::Vector4& rec1, const OSK::Vector4& rec2) {
		return rec1.x < rec2.x + rec2.z && rec1.x + rec1.z > rec2.x && rec1.y < rec2.y + rec2.w && rec1.w + rec1.y > rec2.y;
	}

	bool CheckCircleIntersection(const OSK::Vector2& pos1, const float_t& r1, const OSK::Vector2& pos2, const float_t& r2) {
		float_t dx = pos1.x - pos2.x;
		float_t dy = pos1.y - pos2.y;
		float_t distance = glm::sqrt(dx * dx + dy * dy);

		return distance < r1 + r2;
	}

}*/